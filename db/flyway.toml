[flyway]
locations = ["filesystem:migrations", "filesystem:functions"]

[environments.default]
locations = ["filesystem:migrations", "filesystem:functions"]
url = "jdbc:postgresql://localhost:5432/wow-db"
user = "postgres"
password = "postgres"
